<link rel="stylesheet" href="../css/home.css">

<div class="container-md mt-5">
{{!-- 
    ciclo for que recorra la coleccion grupos, agregar un contador, si el contador llega a 6 que se haga un nuevo
    row
    puedo pasar una coleccion en forma de lista al hbs y asi recorrerla --}}
    <div class="row">
        {{#each groups}}
        <div class="col-md-1">
            <button class="group">{{this}}</button>
        </div>
        {{/each}}

        <form action="/home/addGroup" method="get"  class="col-md-1 p-0">
        <button class="add p-4">
            <img src="../images/add_dark.png" alt="add" title="add" class="group">
        </button>
    </form>
    </div>  
</div>